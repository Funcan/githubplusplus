gh++ git CLI
============

The `gh` CLI from github has loads of great features, but it lacks some
features.

* Listing repos by org and other properties
* Bulk checkout of repos
* Auto-updating forks
